// coerción

// exiten dos tipos:

// conversión automatica o implicita de valores de un tipo dado a otro

const numero1 = "50";

const numero2 = 30;

// convierte numero dos a string
console.log(numero1 + numero2);

// conversión explicita

console.log(Number(numero1) + numero2);

const pedido = [1, 2, 3, 4];

console.log(pedido.toString());
