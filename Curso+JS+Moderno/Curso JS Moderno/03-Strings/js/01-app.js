// Los string o cadena de texto, representan un texto.
// Se puede usar por ejemplo para almacenar el nombre de un cliente.

// Existen tres formas de crear strings
const producto = "Monitor de 20'";

const producto2 = String('Monitor 24 "pulgadas"');

const producto3 = new String("Monitor 32 pulgadas");

console.log(producto);
console.log(producto2);
console.log(producto3);
