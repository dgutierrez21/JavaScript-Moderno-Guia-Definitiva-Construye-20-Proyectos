const producto = {
  nombre: "Mouse Logitech M720 Triathlon",
  precio: 34.99,
  disponible: true,
};

console.log(producto);

// Acceder a las propiedades del objeto usando notacion de punto
console.log(producto.nombre);
console.log(producto.precio);
console.log(producto.disponible);

// Acceder a las propiedades del objeto usando notacion de corchetes

console.log(producto["nombre"]);
