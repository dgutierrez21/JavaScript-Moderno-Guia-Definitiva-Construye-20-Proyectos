// if else

const puntaje = 1000;

if (puntaje == 1000) {
  console.log(
    "Si la condición es verdadera, ejecuta este código TRUE | TRUE | TRUE | TRUE"
  );
} else {
  console.log(
    "Si la condición no es falsa, ejecuta este otro código FALSE | FALSE | FALSE"
  );
}
