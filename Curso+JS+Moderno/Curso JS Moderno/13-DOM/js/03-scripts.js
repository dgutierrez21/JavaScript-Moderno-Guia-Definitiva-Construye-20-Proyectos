const form = document.getElementById("formulario");
//NOTA: Si hay dos elementos con el mismo id, solo va a devolver el primero que encuentre.
console.log(form);

const noExiste = document.getElementById("no-existe");
// Si el elemento no existe, devuelve null

console.log(noExiste);
